<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="/css/add.css">
</head>
<body>

  <form action="/card/edit/{{card._id}}" method="post">
    <label >Name:</label>
    <input type="text" id="name" name="name" value = "{{card.name}}" required>

    <label >Type:</label>
     <select aria-label="select a type" name="type" required> 
        <option value="Monster card ">Monster card </option> 
        <option value="Magic card "> Magic card </option> 
        </select>

    <label >Image:</label>
    <input type="text" id="name" name="image" value = "{{card.image}}" required>

    <label >Origin:</label>
    <input type="text" id="name" name="origin" value = "{{card.origin}}" required>

    <label >Price:</label>
    <input type="text" id="email" name="price" value = "{{card.price}}" required>

    <label >Description:</label>
    <input type="text" id="age" name="description" value = "{{card.description}}" required>

    <button type="submit">Add Card</button>
  </form>

</body>
</html>